<nav class="navbar navbar-default navbar-inverse" data-spy="affix" data-offset-top="100" style="min-width: 275px">
  <div class="container-fluid">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_url %>">
        <%= image_tag "BrasaoJaboticabal.svg.png", style: "width: 40px; display: inline-block; margin-top: -10px " %>
        <%= t 'phone_book' %>
      </a>
    </div>

    <div class="collapse navbar-collapse" id="myNavbar">

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li>
              <%= link_with_icon (t 'navbar.add_phone'), new_phone_path, 'glyphicon glyphicon-plus', { 'data-remote' => true} %>
            </li>
        <% end %>
        <% if logged_in? and current_user.admin? %>
            <li>
              <%= link_with_icon (t 'navbar.manage_users'), users_path, 'glyphicon glyphicon-user' %>
            </li>
        <% end %>
        <li><a href="<%= login_url %>"><span class="glyphicon glyphicon-log-in"></span><%= t 'navbar.login' %></a></li>
      </ul>

      <form method="get" action="<%= phones_search_url %>" class="navbar-form">
        <div class="form-group" style="display:inline;">
          <div class="input-group" style="display:flex;">
            <input class="form-control" name="term" placeholder="Pesquise por Pessoa, Divisão ou Número" autocomplete="off" autofocus="autofocus" type="text">
            <button class="btn btn-default" type="submit">
              <i class="glyphicon glyphicon-search"></i>
            </button>
          </div>
        </div>
      </form>
    </div>
  </div>
</nav>
