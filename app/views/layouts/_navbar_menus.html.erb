<ul class="nav navbar-nav navbar-right">
  <% if logged_in? %>
      <li>
        <%= link_with_icon (t 'navbar.add_phone'), new_phone_path, 'glyphicon glyphicon-plus' %>
      </li>
      <% if current_user.admin? %>
          <li>
            <%= link_with_icon (t 'navbar.manage_users'), users_path, 'glyphicon glyphicon-wrench' %>
          </li>
      <% end %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
          <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
          <%= current_user.name %>
          <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
          <li>
            <a href="<%= logout_path %>" data-method="delete">
              <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>
              <%= (t 'navbar.logout') %>
            </a>
          </li>
        </ul>
      </li>
  <% else %>
      <li><a href="<%= login_url %>"><span class="glyphicon glyphicon-log-in" style="padding-right: 3px"></span><%= t 'navbar.login' %></a>
      </li>
  <% end %>
</ul>