<tr>
  <td><%= phone.person.name %></td>
  <td><%= phone.division.name %></td>
  <td><%= phone.ddr %></td>
  <td><%= phone.ramal %></td>
  <td><%= link_to "Editar", edit_phone_path(phone), remote:true, class: "btn btn-default"%></td>
  <td><%= link_to "Deletar", phone, method: :delete, remote: true, class: "btn btn-danger" %></td>
</tr>